{
  "name": "sentence-repeater",
  "version": "1.0.0",
  "description": "Android app for Sentence Repeater",
  "scripts": {
    "build": "echo 'No build step required for plain HTML'"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor-community/text-to-speech": "6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
